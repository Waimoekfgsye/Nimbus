diff --git a/dom/canvas/WebGLContextValidate.cpp b/dom/canvas/WebGLContextValidate.cpp
--- a/dom/canvas/WebGLContextValidate.cpp
+++ b/dom/canvas/WebGLContextValidate.cpp
@@ -102,6 +102,14 @@
     if (!IsContextLost()) {
+        if (pname == LOCAL_GL_RENDERER) {
+            nsAutoString rendererOverride;
+            if (Preferences::GetString("webgl.renderer-string-override", rendererOverride)) {
+                return StringToJSVal(cx, rendererOverride, rv);
+            }
+        }
+        if (pname == LOCAL_GL_VENDOR) {
+            nsAutoString vendorOverride;
+            if (Preferences::GetString("webgl.vendor-string-override", vendorOverride)) {
+                return StringToJSVal(cx, vendorOverride, rv);
+            }
+        }
         switch (pname) {
